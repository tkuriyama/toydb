module Database.Serialize where

import Database.Database
import Data.Text as T

serialize :: Database -> FilePath -> IO ()
serialize = undefined

deserialize :: FilePath -> Database
deserialize = undefined
